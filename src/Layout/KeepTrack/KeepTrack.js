import React from 'react';
import { Outlet } from 'react-router-dom';

const KeepTrack = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default KeepTrack;